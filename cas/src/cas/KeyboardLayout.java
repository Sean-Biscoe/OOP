package cas; // Package declaration

// Enum representing different keyboard layouts
public enum KeyboardLayout {
    UK, // Option representing the UK keyboard layout
    US; // Option representing the US keyboard layout
}
